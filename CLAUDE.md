# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is in its initial bootstrap state. It contains only `README.md` (a short portfolio description in Korean) and `hello.txt`. There is no build system, package manager, source code, or test suite yet. There are no commands to build, lint, or test at this time.

When code is added to this repo, update this file to document the actual commands and architecture — do not assume a stack until one exists.
